# Build output (bundled entry is committed so Pi can load this git package directly)
dist/*
!dist/index.js

# Backups
*.backup
*.v*-backup

# Dependencies
node_modules
node_modules/

# Lockfile — this project uses bun (see bun.lock). Ignore npm's lockfile
# so an accidental `npm install` / `npm outdated` doesn't create noise.
package-lock.json

# Environment
.env
.env.local

# Coverage
coverage/

# Packages
*.tgz

# Logs
*.log

# OS
.DS_Store

# Scratch / progress files used by sub-agents and chains
progress.md
context.md
chain-runs/

